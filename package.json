{
  "name": "lunaris",
  "version": "1.0.0",
  "description": "Lunaris - Le site de jeux pour vos soirées entre amis",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "build": "esbuild public/js/main.js --bundle --outfile=public/js/bundle.js --format=iife"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
